{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "What the open-browser window may do. `opener` is for revealing a screenshot or a PDF an action wrote; nothing here grants the webview filesystem or shell access.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
