/target
**/*.rs.bk
.DS_Store
node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
/apps/desktop/src-tauri/binaries
# Regenerated by `tauri-build` on every build from `capabilities/` and the plugins' own manifests.
/apps/desktop/src-tauri/gen/schemas
# Browser profiles hold real cookies and logged-in sessions. Never committed.
/profiles
